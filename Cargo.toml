[workspace]
members = [
    "circuit/guest",
    "circuit/host",
    "sdk",
]
# programs/airdrop depends on nssa-core (private LEZ dep) -- not compilable standalone
exclude = ["programs/airdrop"]
resolver = "2"
